# Binários
*.exe
/pokedex

# Estado de runtime (o servidor reescreve; não versionar)
/data/team.json
/data/history.json

# Nota: os dados gerados (data/pokedex.json, data/moves.json, data/sprites/,
# data/types/) SÃO versionados de propósito, para o app rodar hospedado sem
# precisar do jar do Cobblemon.
